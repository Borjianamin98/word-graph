syntax = "proto3";

package ir.ac.sbu.model;

message Page {
    string link = 1;
    string content = 2;
    repeated string anchors = 3;
}

message Anchor {
    string source = 1;
    string destination = 2;
}

message PageKeywords {
    string link = 1;
    repeated string keywords = 2;
}

